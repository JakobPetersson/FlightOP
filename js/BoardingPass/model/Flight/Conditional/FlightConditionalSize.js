import SizeItem from "../../SizeItem";

class FlightConditionalSize extends SizeItem {
    constructor(data) {
        super(17, 'Size of conditional items', data);
    }
}

export default FlightConditionalSize;
import SizeItem from "../../SizeItem";

class AllConditionalSize extends SizeItem {
    constructor(data) {
        super(6, 'Size of variable size field', data);
    }
}

export default AllConditionalSize;
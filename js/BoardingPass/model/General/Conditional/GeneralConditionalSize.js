import SizeItem from "../../SizeItem";

class GeneralConditionalSize extends SizeItem {
    constructor(data) {
        super(10, 'Size of conditional items', data);
    }
}

export default GeneralConditionalSize;